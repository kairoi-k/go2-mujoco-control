# Independent initial joint foothold oracle
This is a frozen actual-state aerial oracle, NOT a full trajectory, terrain coverage, impact or B1 certificate. All four foot targets share one qacc and twelve motor torques; body acceleration is generated by the same articulated dynamics. Source0012 state21.020, fixed touchdown times and zero terminal foot velocities. Targets stay on the old target-height planes in an explicit +/-50mm XY box. This local box is a diagnostic assumption, not a verified terrain patch.
Final result_model_bias.json uses the production go2.xml robot (no external contacts), dense MuJoCo mass matrix and qacc=0 inverse-dynamics bias including passive force/joint friction. The existing C++ frozen-target312.044048851743Nm peak is independently reproduced within1.7e-11Nm. Earlier result.json omitted passive/friction; result_passive.json included passive but still missed0.2Nm friction. These exploratory outputs are superseded. Original code is preserved at d9fb4f6; passive variant replaces -qfrc_bias with -qfrc_bias+qfrc_passive. Final code matches production semantics rather than creating another runtime model.
Formulation: qacc=M^-1(S*tau-h). Per-foot cubic identity p1=p0+T^2/6*(J*qacc+Jdot*v)+2*T*v0/3. HiGHS tests linear feasibility with torque +/-35Nm and target-plane/box constraints. SLSQP minimizes a convex quadratic of foot displacement from old targets, body angular acceleration and small torque regularization. Those weights are diagnostic objectives, not acceptance thresholds. Final residual5.69e-14, peak torque35Nm. Resulting body angular acceleration[-15.23,78.28,6.59]rad/s2 is not a stable body trajectory. An independent additional equality body angular acceleration=0 makes this SAME local problem infeasible (HiGHS status2); not a global impossibility proof.
Reproduce oracle.py or crosscheck.py --source ../joint_execution_flat_20260908/attempt_0012/initial_source.txt --boundaries ../joint_execution_flat_20260908/attempt_0012/swing_boundary_audit.txt --scene unitree_robots/go2/go2.xml --out NEW_JSON, using repo-relative complete paths for both input files. crosscheck prints the additional fixed-target/model/zero-body test. Do not overwrite preserved outputs.
Next: model touchdown velocity and contact transition together with the body/limb trajectory. Zero terminal velocity in7.53ms is a substantive restriction, not a physical axiom. No runtime controller/candidate changes or new simulation in this experiment.

## Conditional free landing-velocity comparison
landing_velocity.py uses the SAME source, mass/bias, timing, target planes/box and35Nm envelope, adding four normal terminal velocities in[-2,0]m/s and constraining body angular acceleration to zero. Tangential terminal velocity remains zero. The cubic identity adds T*v1/3 to target position. HiGHS feasibility and SLSQP QP both succeed, residual1.13e-13. Normal velocities [0,-0.0000433,-0.249892,-0.018435]m/s suffice; peak torque35Nm. The previous zero-normal-terminal-velocity/zero-body-acceleration LP is infeasible under the otherwise same constraints. This identifies an unnecessarily restrictive local boundary assumption; it does not validate impact or all-horizon feasibility.
Run landing_velocity.py with the same CLI and a new --out auxiliary baseline file, capturing stdout into a new result file. Landing result provenance uses the same source/boundary/model hashes as result_model_bias.json; its own script hash is included. No chosen velocity bound is an acceptance threshold. Normal target velocity is a pre-contact quantity; do not silently carry it into a stationary stance or reset measured state. Next explicit contact-transition dynamics must connect pre/post-contact velocity, force/impulse and body motion, and be checked against MuJoCo. No controller actuation change here.
