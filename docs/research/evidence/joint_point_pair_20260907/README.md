# Flat contact-point WBC pair
This is a reproducible diagnostic packet for two 32 s flat-ground runs at clean runtime `e1e68de1cee4edb93d1094a39a52c8e4bb5347ec`. Both use the same scene, V3 profile, seed 11, DDS domain 231, running-trot settings, and 0.14 s period. `off` records `TROT_RESEARCH_CONTACT_POINT_MODEL=0`; `on` records `=1`. The raw manifests report clean source and matching controller, simulator, scene, and profile hashes. Raw files remain in their original `_runs` directories; their hashes are in `results.json`.
Replay from the repository root with the existing adapter:
    python3 docs/research/evidence/joint_articulated_20260907/reproduce.py example/cpp/experiments/_runs/joint_point_flat_off_20260907_0001 --runtime-sha e1e68de1cee4edb93d1094a39a52c8e4bb5347ec --point-model 0 --out /tmp/joint_point_pair_reproduce_off_e1e68de
    python3 docs/research/evidence/joint_articulated_20260907/reproduce.py example/cpp/experiments/_runs/joint_point_flat_on_20260907_0001 --runtime-sha e1e68de1cee4edb93d1094a39a52c8e4bb5347ec --point-model 1 --out /tmp/joint_point_pair_reproduce_on_e1e68de
The replay adapter, certificate analyzer, and cycle analyzer are reused; no new large harness is introduced. An independent rerun matched the supplied certificate and cycle output hashes recorded in `results.json`.
Using `telemetry_gait_time_s` 18-24, `off` has 40/42 good cycles and 210 selected-certificate failures. `on` has 33/42 good cycles and 372 failures. Failure masks are off: 49 normal-only, 156 friction-only, 5 both; on: 57, 306, 9. Both have zero swing-force violation rows and 18552/18552 exact force-point provenance rows for the expected mode. The physical certificate is diagnostic-only and excludes final-PD torque.
Every raw period value in the 18-24 gait window is exactly 0.140000000. The old isolated `43c5f5a` run was audited over 17-24 and therefore has 49 complete cycles; its raw period in that window is also exactly 0.140000000. The count difference is the seven-second versus six-second analyzer window, not a 0.12-period run.
Raw WBC certificate, WBC shadow, and LiDAR timing quantiles are recorded in `results.json`. Negative LiDAR publish values are the raw no-publication sentinel and are excluded from nonnegative timing quantiles. The new raw files contain no scheduler/build/archive event marker, so concurrent-load contamination is unknown from these files. The historical packet records old run 0002 as isolated; that is registration context rather than raw scheduler proof.
Both manifests have controller, safety, completion, quality, dynamics, and truth status zero. Phase-1 and terrain wrapper statuses are one because the profile and flat-scene analyzers fail independently; certificate return code 1 matches `NOT_CERTIFIED`, while cycle analysis returns zero. This packet makes no B1 acceptance or joint-planner execution claim.
