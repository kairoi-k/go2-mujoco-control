Independent review (2026-09-07; read-only audit, except the telemetry fix below)
Scope: /home/che/dev/go2-workspace/feat-stage-c-joint-planner. Read AGENTS.md and CURRENT.md. HEAD=0ff9dc8; worktree was already dirty. No simulation was run. Only the focused test_stage_c_world_terrain_snapshot target was rebuilt and run; it passed.
F1 (concrete blocker; main agent should fix): world_terrain_snapshot.h:384-389 returns kOutside immediately when !model.CoversPatch, so it never scans the query/grid overlap. SampleWorldTerrainSnapshot:686-711 only calls ConflictBetween for entries with any_fresh_known. A newer capture at the map edge, with a fresh contradictory known overlap, is therefore ignored and the older complete map can be selected at 714-729. This violates the joint_terrain_history README rule that fresh partial observations conflicting beyond tolerance reject the query and CURRENT fail-closed coverage. Repro reasoning: old map fully covers query at z=0; newer map intersects only one side of the query window at fresh z=+0.01; CoversPatch=false makes any_fresh_known=false, so old map returns valid. Fix must keep covers_patch=false to prevent selection, but scan overlapping cells and include them in conflict checks; add a boundary partial-overlap test.
F2 (concrete provenance blocker; main agent decision): WorldTerrainSnapshot::ok() 104-117 checks metadata only, and Sample 671 does not validate each entry. InspectModel 361 onward checks TerrainModel metadata but not capture provenance. A manually constructed valid=true snapshot with a valid base_link registered model and finite registration pose/grid but NaN capture pose or mismatched provenance still returns a valid patch. BuildWorldTerrainSnapshot 626-644 protects normal construction but not the public direct-struct path. CaptureTerrainViewResult.scope is also not checked at Build 626-644, so an unknown scope can enter a capture-heading snapshot. Build should check scope; Sample should reject any CaptureHeadingEntryValid failure.
Telemetry fix made: conflict return now sets result.diagnostic.failure=kConflictingHistory (world_terrain_snapshot.h:709-712), with a new assertion (test_stage_c_world_terrain_snapshot.cpp:156-162). This fixes JointPlanningShadow initial-query logs that could previously report reason=none. Focused target passed.
