# Original swing-phase preservation
Runtime f64d4fb. Opt-in preserve_inflight_clearance_phase keeps actual source position/velocity and original liftoff/touchdown times. The residual quartic amplitude is clearance*((touchdown-source)/(touchdown-liftoff))^4. This is the exact Hermite remainder of the original16*s虏*(1-s)虏 bump, not a new full-height swing restart or a local timing policy. It takes precedence over the old added-clearance boolean only when explicitly enabled. Existing defaults remain unchanged.
Independent scalar identity check over phases0.05/0.5/0.9 gives error<4e-16. Focused C++ tests compare full versus reseeded tilted-normal curves at21 points per phase for p/v/a and verify the actual Prepare plumbing; all pass. Original foot-trajectory fixtures pass. Preserving a mathematical curve is not a terrain collision or robot feasibility certificate.
Read-only --articulated-phase-audit on0012 source changes initial torque peak312.044->310.601Nm, still over35Nm; no promotion or new closed-loop run. Runtime controller switch is not wired. The new curve semantics must be included in future joint-target optimization rather than enabling an unchecked new reference. Reproduce replay_joint_shadow_snapshot INITIAL_SOURCE --articulated-phase-audit; source is attempt_0012/initial_source.txt. The initial actual q/dq and event times are unchanged.
