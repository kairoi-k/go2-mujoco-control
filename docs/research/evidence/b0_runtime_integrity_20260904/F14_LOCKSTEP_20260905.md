# F14 lockstep diagnostic - 2026-09-05

Status: diagnostic only; not B0 or B1 acceptance.

## Source and runs

- Source SHA: 8f4a58176abcae6ed94a20f8230e236f2990f18d, detached clean main worktree.
- Worktree: /home/che/dev/go2-workspace/tmp/main-baseline.
- Baseline run: /home/che/dev/go2-workspace/tmp/main-baseline/example/cpp/experiments/_runs/f14_lockstep_main_baseline_20260905.
- Terrain run: /home/che/dev/go2-workspace/tmp/main-baseline/example/cpp/experiments/_runs/f14_lockstep_main_terrain_20260905.
- Both used SIM_LOCKSTEP=1, a 2 ms exchange, 40 s duration, and the experiment lock. The terrain member used terrain sensor-only with no actuation.

## Observed

- Baseline lockstep trace: 28,594 rows, continuous 2 ms spacing, no fail-closed.
- Terrain lockstep trace: 27,869 rows, continuous 2 ms spacing, no fail-closed.
- The standalone sustained-running analyzer returned validation PASS for both members. The baseline good window was 43.018 s; terrain was 42.240 s.
- The Phase 2 analyzer reported top-level acceptance PASS for this fixed-3-m/s contract, but its paired diagnostics were false: gait duty max difference 0.443248827, gait period max difference 0.020000000 s, requested acceleration max difference 9.089620904 m/s2, and WBC velocity-target max difference 3.000000000 m/s.

## Interpretation and decision

The shared tick exchange and barrier are mechanically healthy and remove the hard posture collapse seen in the earlier wall-clock terrain slice. They do not make the baseline and terrain command or trajectory series identical, and this is not a fresh full wall-clock B0 result. Keep F14 active: define the state/command/analyzer time-index contract, add the focused contract assertion, then rerun the complete exact-SHA B0 suite. Do not promote this diagnostic or start B1.

## Exact state/ack guard and canary - 2026-09-05

- Source SHA: 6cdf2366bb38aab9db29184efc813be71ae3022f, detached clean worktree
  `/home/che/dev/go2-workspace/tmp/f14-guard-candidate`.
- The production writer now receives the tick returned by `WaitForTick`;
  `LowCmdWrite(expected_state_tick, true)` snapshots the state and requires the
  exact same `LowState.tick()` before publishing a command or acknowledgement.
  A mismatch raises `kViolationSnapshotMismatch` and fails the lockstep gate closed.
- Exact baseline run: `/home/che/dev/go2-workspace/tmp/f14-guard-candidate/`
  `example/cpp/experiments/_runs/f14_guard_exact_baseline_20260905`.
- Exact terrain sensor-only run: `/home/che/dev/go2-workspace/tmp/`
  `f14-guard-candidate/example/cpp/experiments/_runs/f14_guard_exact_terrain_20260905`.
- Both 20 s members exchanged continuous 2 ms traces without fail-closed:
  baseline 19,200 rows and terrain 18,958 rows. Both standalone analyzers
  returned `validation=PASS`; the manifests record the exact clean SHA.
- The fixed-3-m/s Phase 2 analyzer returned top-level `acceptance_status=PASS`,
  but paired diagnostics remained false: duty max difference 0.331575057,
  period max difference 0.020000000 s, requested acceleration max difference
  5.184503362 m/s2, and WBC velocity-target max difference 2.875004202 m/s.
- No snapshot-mismatch or writer fail-closed diagnostic occurred in either canary.
- Decision: the exact state/ack guard is mechanically healthy, but command and
  trajectory equivalence is still unresolved. Keep F14 active; this canary is
  not full B0 acceptance and does not authorize B1.

## Rejected early-handoff probe

- A temporary probe moved the lockstep epoch to the first ack before gait.
  It consumed repeated snapshots with `motion_dt=0`, never left stand, and
  ended by wall-time kill (`controller_status=137`). The temporary source was
  restored clean; raw run: `/home/che/dev/go2-workspace/tmp/f14-early-handoff/`
  `example/cpp/experiments/_runs/f14_early_lockstep_baseline_20260905`.
- Keep the start-gait boundary until the exact state/ack pairing is redesigned.

