#include "diagnostic_schema.h"
#include <cassert>
#include <string>
int main(){using namespace go2_diagnostic;assert(std::string(TopicName(Topic::kLowCmd))=="rt/lowcmd");CaptureRecord lowcmd;lowcmd.topic=Topic::kLowCmd;assert(lowcmd.payload_repr==PayloadRepresentation::kAbsent);assert(lowcmd.source_message_id.empty());assert(lowcmd.serialized_payload.empty());assert(lowcmd.complete_decoded_value.empty());assert(lowcmd.source_stamp_valid==false);assert(lowcmd.publication_handle_or_guid.empty());assert(lowcmd.source_id_valid==false);assert(lowcmd.publication_identity_valid==false);MapRecord m;m.capture_id=7;m.width=2;m.height=1;m.complete_value=true;m.map_stamp_valid=true;m.frame_valid=true;m.frame_id="base";m.cells={{0,0,1,true,0,true},{1,0,2,true,0,true}};assert(m.valid());StateRecord s;s.capture_id=8;s.state_stamp_valid=true;s.frame_valid=true;s.pose_valid=true;assert(s.valid());assert(!ConsumerAck::Absent().valid);}
