<mujoco model="go2 low friction patch">
  <include file="go2.xml"/>

  <statistic center="0.7 0 0.18" extent="1.4"/>

  <visual>
    <headlight diffuse="0.7 0.7 0.7" ambient="0.35 0.35 0.35" specular="0 0 0"/>
    <rgba haze="0.15 0.25 0.35 1"/>
    <global azimuth="130" elevation="-16"/>
  </visual>

  <asset>
    <texture type="skybox" builtin="gradient" rgb1="0.3 0.5 0.7" rgb2="0 0 0"
      width="512" height="3072"/>
    <texture type="2d" name="groundplane" builtin="checker" mark="edge"
      rgb1="0.2 0.3 0.4" rgb2="0.1 0.2 0.3" markrgb="0.8 0.8 0.8"
      width="300" height="300"/>
    <material name="groundplane" texture="groundplane" texuniform="true"
      texrepeat="5 5" reflectance="0.12"/>
    <material name="low_friction_patch" rgba="0.08 0.20 0.78 1"/>
  </asset>

  <worldbody>
    <light pos="0 0 1.5" dir="0 0 -1" directional="true" castshadow="false"/>
    <geom name="floor" size="0 0 0.05" type="plane" material="groundplane"/>
    <!-- A flush, collidable patch: normal floor -> low friction without a
         scripted friction signal. The controller must infer the change from
         support-foot motion. -->
    <geom name="low_friction_patch" pos="0.80 0 0.0005" type="box"
      size="0.80 0.60 0.0005" material="low_friction_patch"
      friction="0.005 0.005 0.0005" contype="1" conaffinity="1"/>
  </worldbody>
</mujoco>
